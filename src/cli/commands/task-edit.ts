/**
 * Task edit command
 */

import { logger } from '../utils/logger.js';

export async function taskEditCommand(_id: string, _options: unknown) {
  logger.warning('Task edit command not yet implemented');
  logger.info('Use the MCP tool tasks_update for now');
}
